import {client} from '@/sanity/lib/client'
import {frontendModules} from '@/modules/generated'
export default async function Search({searchParams}:{searchParams:Promise<{q?:string}>}){const{q=''}=await searchParams;const core=q?await client.fetch<any[]>(`*[_type in ["page","post","category"]&&(title match $q||excerpt match $q||description match $q)][0...30]{_id,_type,title,slug,excerpt,description}`,{q:`*${q}*`}):[];const extra=q?(await Promise.all(frontendModules.map((m)=>m.search?.(q)||Promise.resolve([])))).flat():[];const items=[...core,...extra];return <section className="collection-page"><div className="section-inner width-contained"><h1>Search</h1><form className="search-form"><input name="q" defaultValue={q} placeholder="Search this website"/><button className="button">Search</button></form>{q&&<p>{items.length} results</p>}<div className="search-results">{items.map((x:any)=><article key={x._id}><span className="eyebrow">{x._type}</span><h3>{x.title}</h3><p>{x.excerpt||x.description}</p></article>)}</div></div></section>}
